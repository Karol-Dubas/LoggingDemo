{
  "Logging": {
    "Console": {
      "LogLevel": {
        "MyCategory": "Information" // disable in console
      }
    },
    "LogLevel": {
      // Categories:
      "Default": "Trace", // base level
      "Microsoft": "Warning", // whole namespace/category, more specific rule
      "Microsoft.AspNetCore": "Information", // this specific overrides all these above
    }
  }
}
